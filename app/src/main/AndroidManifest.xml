<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.IThrift"
        android:usesCleartextTraffic="true">
        <!--
          usesCleartextTraffic is enabled because this app talks to a local
          development server over plain http:// (e.g. http://10.0.2.2:3000
          or http://<your-PC-LAN-IP>:3000). It is fine for this prototype,
          which only ever points at a server you control on your own
          network. A production app calling a public server should use
          https:// and remove this flag.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
